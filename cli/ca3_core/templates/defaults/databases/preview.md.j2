{#
  Template: preview.md.j2
  Description: Generates a preview of table rows in JSONL format

  Available context:
    - table_name (str): Name of the table
    - dataset (str): Schema/dataset name
    - db (DatabaseContext): Database context with helper methods
#}
{% set rows = db.preview() %}
{% set active_filter = db.active_partition_filter() %}
# {{ table_name }} - Preview

**Dataset:** `{{ dataset }}`

{% if active_filter %}
> Filtered by partition: `{{ active_filter }}`

{% endif %}
## Rows ({{ rows | length }})

{% for row in rows %}
- {{ row | to_json }}
{% endfor %}
